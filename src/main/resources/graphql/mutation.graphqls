scalar NonNegativeInt
scalar DateTime
scalar Date

# All mutation available in graphql
type Mutation {
    # Create a bank account
    createBankAccount(input: CreateBankAccountInput!): BankAccount!
    # Upload a file
    uploadFile: ID!
}
